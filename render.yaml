
services:
  - type: web
    name: waveform-3000
    env: python
    buildCommand: "apt-get install -y nodejs ffmpeg && pip install -r requirements.txt"
    startCommand: "gunicorn app.main:app"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        sync: false
